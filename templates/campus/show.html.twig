{% extends 'base.html.twig' %}

{% block title %}Campus{% endblock %}

{% block body %}
    <h1>Campus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ campus.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ campus.description }}</td>
            </tr>
            <tr>
                <th>Lat</th>
                <td>{{ campus.lat }}</td>
            </tr>
            <tr>
                <th>Lon</th>
                <td>{{ campus.lon }}</td>
            </tr>
            <tr>
                <th>LocationName</th>
                <td>{{ campus.locationName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('campus_index') }}">back to list</a>

    <a href="{{ path('campus_edit', {'id': campus.id}) }}">edit</a>

    {{ include('campus/_delete_form.html.twig') }}
{% endblock %}